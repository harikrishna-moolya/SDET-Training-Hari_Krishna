[pytest]
markers =
    smoke: Quick smoke tests
    regression: Full regression tests
    slow: Tests that take longer to run
    unstable: Tests that are known to fail intermittently

# HTML reporting defaults
addopts = --html=reports/report.html --self-contained-html -n 2